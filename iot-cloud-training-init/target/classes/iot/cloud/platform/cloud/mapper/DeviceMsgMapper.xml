<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="iot.cloud.platform.cloud.mapper.DeviceMsgMapper">
    <select id="getByTag" resultType="iot.cloud.platform.cloud.entity.DeviceMsgEntity">
        SELECT id, iot_id, msg, tag, create_time
        FROM device_msg
        WHERE iot_id = #{iotId} AND tag = #{tag}
        ORDER BY create_time DESC
    </select>
    <insert id="save" parameterType="iot.cloud.platform.cloud.entity.DeviceMsgEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO device_msg (iot_id, msg, tag, create_time)
        VALUES (#{iotId}, #{msg}, #{tag}, #{createTime})
    </insert>
</mapper>